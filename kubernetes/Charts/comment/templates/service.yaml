---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "example-comment.fullname" . }}
  labels:
    app: example-app
    component: example-comment
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.externalPort }}
      protocol: TCP
      targetPort: {{ .Values.service.internalPort }}
  selector:
    app: example-app
    component: example-comment
    release: {{ .Release.Name }}
